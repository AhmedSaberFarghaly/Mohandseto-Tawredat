# Milestone 50% — M5 Complete

اكتملت الموافقات الداخلية وRFQ للشاشات 152–203.

## RFQ المنجز

- إنشاء من الكتالوج أو صنف حر أو Excel/CSV/PDF/صورة.
- استخراج XLSX/CSV فعلي، ومسار extraction adapter قابل للاستبدال لـPDF والصور، مع مراجعة إلزامية.
- مواصفات وكميات ووحدات وبدائل وربط بمنتج الكتالوج.
- نشر عرض السعر بصلاحية وتوريد وضريبة وشحن وإصدارات ثابتة.
- مقارنة إصدارات وبدائل، تفاوض ورسائل وعرض مقابل، قبول ورفض وإعادة تسعير.
- PDF صالح، مشاركة ملخص، انتهاء صلاحية، وتحويل العرض المقبول إلى Order مع الميزانية والموافقات.
- نقاط إدارة نشر العرض محمية بأدوار `platform_admin`, `quotes_officer`, `sales_manager`.

## التحقق

- 28 اختبار Backend و11 Flutter ناجحة.
- tenant isolation، extraction، إصداران، تفاوض، PDF وتحويل Order مغطاة آليًا.
- migration `RfqWorkflow` مطبقة على قاعدة فارغة و`flutter analyze` نظيف.
